# Example .env file for Jira Creator App

# --- Ollama Configuration ---
# Specify the host and port where your Ollama instance is running.
# If running on the same machine, localhost is usually correct.
# Ensure it includes the port number (default is 11434).
# Example: OLLAMA_HOST=http://localhost:11434
# Example: OLLAMA_HOST=http://192.168.1.100:11434
OLLAMA_HOST=http://localhost:11434

# --- Optional: Flask Configuration (Defaults are usually fine) ---
# FLASK_APP=src/main.py
# FLASK_RUN_PORT=5000
# FLASK_DEBUG=0 # Set to 1 for development debugging, 0 for production/normal use

